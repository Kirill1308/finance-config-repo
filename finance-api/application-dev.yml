spring:
  cloud:
    gateway:
      routes:
        - id: auth-service
          uri: http://localhost:8081
          predicates:
            - Path=/api/auth/**
        - id: wallet-service
          uri: http://localhost:8082
          predicates:
            - Path=/api/wallets/**

logging:
  level:
    com.popov: DEBUG
