spring:
  redis:
    host: localhost
    port: 6379
    database: 0
    timeout: 60000
  jpa:
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.popov: INFO
    org.hibernate.SQL: DEBUG
